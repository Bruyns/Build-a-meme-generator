import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App"

ReactDOM.createRoot(document.getElementById("root"))
.render(< App />)

// used createRoot because the ReactDOM.render kept giving errors in the console so i decided to use the 18 change